/**
 * Javadoc for class
 */
class ClassWithDocs {

    /**
     * add [] because exact version can be represented as [<fails_here>]
     */
    private val someVal = "anything"

    /**
     * add [] because exact version can be represented as \[<it_is_ok>\]
     */
    private val someValFixed = "anything"


    /**
     * Javadoc for someFun
     */
    fun someFun() {

    }
}